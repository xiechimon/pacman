{
  "name": "@pacman/cli",
  "version": "0.13.3",
  "license": "Apache-2.0",
  "description": "Terminal client for the Pacman server",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "pacman": "src/index.ts",
    "craft-cli": "src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test src/"
  },
  "dependencies": {
    "@pacman/shared": "workspace:*",
    "@pacman/server-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "@types/node": "^22.0.0",
    "@types/bun": "latest"
  }
}
