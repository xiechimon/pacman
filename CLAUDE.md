# Pacman

Claude Code project instructions.

## Agent skills

### Issue tracker

Issues for this repo are tracked as GitHub issues in xiechimon/pacman, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary; label strings equal the role names. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` glossary at the repo root, ADRs under `docs/adr/`. See `docs/agents/domain.md`.
