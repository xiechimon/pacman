# 05 · M6 功能平价终验 · 验收报告（#232）

> 正典地位：本册 = 03 册 §2 M6 出口要求的验收报告落盘面（`docs/spec/`）。
> 口径单源 = 04 册；栈与 pin = 01 册；架构义务 = 02 册；构建顺序 = 03 册。
> 实跑环境：darwin arm64 / Node 26.8.1 / pnpm 12.5.1 / ffmpeg 9.0.2（本地全矩阵）；
> CI 面 = ubuntu Node 24（ci.yml 既有门禁）。
> 执行日：2026-09-25（#232，Q1 裁决 A = 全口径重跑；2026-09-23 首轮兑现记录压附录 A 一行指针）。

## 0. 结论一览

| # | 项 | 结果 |
|---|---|---|
| V1 | web 全已建屏走真 API（fixture 仅 dev/parity 存活） | ✅ 15 条路由 + chief 双面 + ⌘K 面板 live 接线；资源四族创建弹窗（provider/secret/mcp/agent）与机器添加弹窗（#181）落地；反馈路由 #149 裁决整除（未匹配路径回落 catch-all 重定向）；`?scenario=` 退役为 dev/parity-only（#58 gate，生产 build 编译期折叠） |
| V2 | live streaming 接真 SSE | ✅ conversation stream（词表内端点补实现）四事件定型；daemon text_delta 节流转发；E2E 断言 text_delta/message/step 三族齐至 |
| V3 | E2E 主时序全链绿（01 §7.4 脊柱） | ✅ integration/test/m5-web-e2e.test.ts 3/3：主时序 + 驳回支线 + 定时轮停 review，UI 零 reload（本票重跑 9.57s） |
| V4 | 像素全矩阵（含 #229 弹层族 22 行）0 失败 | ✅ 229 行全绿（本票实跑，见 §2） |
| V5 | wire 对拍全绿 | ✅ 词表全端点在位 + schema 快照面；vitest 全量 52 文件 / 505 用例绿（见 §3） |
| V6 | 04 §4 行为清单逐条核 | ✅ 见 §4 逐行核表 |
| V7 | 真人一次项 | ✅ 三项全兑现：OS 通知权限 + 桌面通知实弹（09-23 兑现，附录 A；本票按全口径精神阶段重确，#114 banner 落地后权限入口为应用内）+ OAuth 真连号（2026-09-25 ego 联调实跑兑现，§5 兑现记录）——V7 等价注记随连通摘除 |
| V8 | 升级票开窗 | ✅ #104–#107 四票已闭（#115/#117/#118/#119 落地，窗口事实见 §7） |

## 1. M5 汇合实现纪要

### 1.1 数据源缝（一个显示契约、两个数据源）

- 显示契约（`apps/web/src/fixtures/records.ts`）= 像素矩阵冻结的组件 props 面，**不动**；
  live 数据层（`apps/web/src/api/`）把 02 §6.2 wire record 投影成同一显示契约：
  - `client.ts` 同源 fetch（错误形状 `{error}` 单形状，04 §3）；
  - `hooks.ts` TanStack Query v5（01/S8 pin 5.103.1）查询/变更面，
    失效重取为唯一 server-state 策略；
  - `sse.ts` 原生 EventSource 双通道（team stream → invalidateQueries 提示信号 =
    S8 canon；conversation stream → transcript live 面）；
  - `mappers.ts` wire→display 投影（transcript 组装/plan.md 段落化/unified diff
    行号展开/token 计数形/运行历史行/chief 流）；
  - `live-text.ts` text_delta 打字缓冲（useSyncExternalStore；终稿 message 行落库
    即清空收敛——02 §1.2 双保险的流侧半）。
- 模式闸门（`api/mode.ts`）：`?scenario=` 且 dev/parity build → fixture；其余一切
  （含生产 build 恒真）→ live。parity 矩阵与既有 reject-chain e2e 的 fixture
  数据面零改动。
- 桌面通知 divergence 兑现（04 §5/A5）：SSE notification 事件 → `document.hidden`
  时页内 `new Notification()`；无 Web Push/VAPID（登记口径，不判负）。

### 1.2 server 补齐面（词表内 + [推断] 登记）

- **conversation stream**：`GET /api/conversations/{id}/stream`（词表内）。
  事件定型四件 = ping / message / text_delta / step（载荷 [推断]，shared
  `conversationStreamEventSchema` 单源，快照冻结；官方逐事件载荷未枚举——补采后
  回写 02 §11 收紧）。发布点：live 工具行（reportTool）、transcript 终稿
  （receiveUpload）、步状态（enqueue/claim/finish）、驳回用户行、合并行、🎉 行、
  chief 消息行。
- **daemon live 文本转发**：pi `text_delta` 250ms 窗口聚合 → `POST tool/{stepId}`
  第三形 `{kind:"transcript_delta",text}`（[设计] 登记 shared machine-wire.ts；
  13 端点词表路径不改形状）；服务端瞬态转发不落库，终稿经 upload-urls 兜底。
- **词表补齐 GET/POST 族**（02 §6.1 canonical、03 §5「M5 wire = 全端点」）：
  teams/{id}/machines · models · progress · skills/{sid}(+/file) · agents/{aid}/tasks、
  GET/POST /api/skills、whats-new、analytics/first-touch + /_mp/api/track（词表
  「可空实现」= 204 收下即弃）。preview-token 不实现 = 04 附录 B「在册不设计」
  （无 UI/wire 触点，触发条件未至）。
- **[推断] build 详情读面**（wire.test INFERRED_ROUTES 登记，路径 = builds/{id}/…
  REST 同族规则）：plans（版本集+内容，r5 §4 触点）· changes（conv 分支 vs 默认
  分支 unified diff，r7 27 触点）· changes/file（按需单文件全文，#224）·
  usage（token 四维，r3 §3.8/r7 30 触点）。steps 读面行透出 journal 位
  status/checkpointCommit（[设计]，zod strip 下 stepRecordSchema 对拍不漂移）。
- **SPA 静态同源托管（02/A1）**：ctx.webDir → 非 API GET 静态文件/SPA 回退
  index.html；/api、/git、/_mp 未命中恒 404 JSON（错误形状对拍面不破）；路径逃逸
  拒绝；index no-cache + 资产 1h。E2E 与生产形态同构（同源 cookie/SSE 免代理）。
- **OAuth 握手面（#231）**：`POST /api/teams/{id}/providers/oauth/{preset}/authorize`
  签发 + `/api/oauth/callback` 收码 302 回跳（token 永不进 redirect，结果经
  `?oauth=connected|error` 查询参传递）；族表 = shared `OAUTH_FAMILIES` 单源，
  现行一族 = github-copilot（授权 URL/token URL/scope/providerBaseUrl 四件）。
- **GitHub 出站面（#223）**：出站 fetch 薄桥 + `POST /api/skills/scan` 扫描发现半
  （技能导入面数据源）。

### 1.3 live 接线的已建屏清单（V1 逐屏）

| 屏 | live 数据面 | live 写面 |
|---|---|---|
| 看板（board） | todos/projects/machines/notifications/chief+threads（SSE 直更） | 新建任务、保存并开始、卡面 开始、拖拽排序（orderIndex PATCH）、review 完成→accept→merge、编辑分配（#209，PATCH assignment 双槽） |
| 详情（todo detail） | todo/build/steps/messages/plans/changes/changes-file/usage/machines + conversation stream（打字面） | 开始/确认/驳回（composer→revision）/验收合并/重跑（先做规划·立即执行·复用方案=直执行）/删除；docpane「显示完整文件」钮接 #224 按需全文端点（#225） |
| 定时（schedules） | GET /api/schedules（SSE todo 联动） | 新建定时（频率 tab+分档受控 → POST；02 §9.2） |
| 机器 | GET teams/{id}/machines（+托管行 canon） | 添加机器弹窗（#181，r2 11b CLI 两步表单：安装/执行命令块带复制 + API key disclosure 内嵌 teamId 链）；行内三动作图标出账（#222，端点核实测无 local-first 机器管理面，不渲染死钮） |
| 模型服务 | GET providers 封套（+内置行 canon） | 创建弹窗 live（POST providers）；「连接订阅」段 = #231 OAuth 一族入口（github-copilot，authorize → 同页签跳授权页 → 回跳列表行） |
| 密钥（secrets） | GET secrets（有行 = 资源行卡语言呈现 [设计]） | 创建弹窗 live（POST secrets，写后不读口径） |
| 技能 | GET /api/skills | 新建技能：文件夹目录选择 → 文件集 POST（SKILL.md 必含 = server 400 同款）；GitHub 扫描导入（#233 端点 + #235 钮接线：scan 双模式消费，候选列表选中即导入） |
| MCP 服务器 | GET mcp-servers | 创建弹窗 live（POST mcp-servers，http/stdio 双形态，#174 dlg-seg 裁决） |
| 团队 | GET members（Agent 卡网格 = actor 投影） | 创建 Agent 弹窗 live（POST agents） |
| API 密钥 | GET api-keys（掩码行） | 新建密钥 = 默认权限位直发 + 一次性明文块（r3 §6 权限位表单弹窗 = 后票，§6） |
| 帐号 | GET /api/user/me（名称行）；邮箱 = 占位破折号（复刻无邮箱账位面 [设计]） | 语言 dropdown（#74）；推送通知 switch（镜像 Notification.permission，#114 同路径） |
| 项目 ×3（详情/设置/新建） | GET projects/tree + todos 过滤 | 新建项目（POST → hosted bare 落地 → 跳项目页）；文件查看器（#202，/file 按需取，encoding 分渲）；设置删除区（#207，DeleteProjectConfirm 键入项目名闸门 → DELETE /api/projects/{id} 级联删） |
| 总管 drawer | GET chief/threads + 活动线程消息 + conversation stream | 发消息（新主题 POST threads / 续消息 POST conversations messages）；主题切换 |
| 总管设置 | live = canon 静态面呈现（绑定态/模型槽真值） | Agent 绑定弹窗、章程编辑弹窗、压缩模型 anchored 菜单（#204，PATCH compactionModel 槽）；记忆/关注两 tab = 占位文案 canon |
| ⌘K 搜索面板 | live todos 客户端过滤（真数据）；项目 chip 真名 | GET /api/search 端点在位未消费（面板契约 = 客户端过滤，§6） |
| 反馈 | 路由整除（#149 local-first 裁决，#129 先例）——旧路径回落 catch-all 重定向 | — |

卡面/搜索/新建 dialog 的项目 chip = `FixtureSet.projectNames` 位（live 真项目名；
fixture 面缺省走 capture canon 常量，矩阵零漂移）。

### 1.4 M6 窗口增量（09-23 首轮后至本票开工落地，全量重跑的动因）

- batch2：#221 弹窗样式族整族抽取（六弹窗 CSS 收 .dlg-form-* shared 层）、
  #223 GitHub 出站 fetch 薄桥 + skills/scan 端点、#224 diff 全文读面端点、
  #225 docpane「显示完整文件」钮接线（#237 合入）。
- #229：弹层族 open 态 22 行入 parity 门禁（8 弹窗 11 面 light+dark，M6 Q3 裁决；
  #172「弹窗不入 parity」旧裁决废止）。
- #230：provider/agent/mcp 三族 POST body schema 上收 shared 单源
  （create/patch 六件，routes.ts 删本地定义改 import 消费，web mutation 位收型）。
- #231：OAuth 握手面 github-copilot 一族打通 + web 连接订阅入口 + 连号 wizard
  （`scripts/connect-github-copilot.sh`）。
- #233/#235：技能导入面 GitHub 扫描（端点 + 钮接线）。
- 同窗口其余票：#181 添加机器弹窗、#202 项目文件查看器、#204 chief 压缩模型
  翻回交互、#207 项目删除区复活、#209 编辑分配接线、#222 机器行内图标出账、
  #216 runGit stdin EPIPE flake 修复、#194 e2e inline import 清扫。

## 2. 像素平价层（V4）

- 矩阵：**229 行全量实跑 0 失败**（本地 darwin，`PARITY_PORT=8390`；产物存档
  parity/output/report.json，gitignored）。
- 构成：67 门禁行（0.85 档 63 + 0.84 档 4）+ 143 smoke 行（SSIM=1.0 自比对 +
  expectText 内容齿）+ 19 report-only 行（出报告不门禁）。
- 0.84 档现行四例（行注登记理由 + blend 人工核无结构差异，04 §2 规则）：
  `detail-confirm-light` / `detail-usermenu-click-light` / `detail-done-light`
  （跨平台字体栅格化噪声）+ `detail-failed-light`（CJK 折行点随渲染器字体栈
  漂移）。04 §2 括注「现行两例」为 #45 时点快照，后两例随票登记、口径不变。
- 本票最低分门禁行：`detail-failed-light` 0.8459 @ 0.84（余量 0.006，已知
  CJK 折行漂移面，登记在案）。
- 基线改动前后双跑：本票为纯验收票（零 app 代码改动），全量单跑即基线；
  开工点 = origin/main @ a2773a13（#229/#230/#231/#233/#235/#237 全落）。
- 阈值体系 = 04 §2 原样，本票无新增 override、无新增 report-only。
- CI 门禁（drizzle drift + typecheck + vitest + lint + parity + web e2e 全量）=
  ci.yml 既有步骤，无改动。

## 3. wire 层（V5）

- 词表对拍（apps/server/test/wire.test.ts，22 用例）：M2a 核心面 / M2c 密钥搜索
  面 / M5 汇合面（M5_ROUTES：词表补齐族 + conversation stream + `/_mp/api/track`
  显式在位）/「无词表外发明路径」全绿；INFERRED_ROUTES 登记四行 = plans /
  changes / usage / changes/file（#224 全文读面同族登记）。
- schema 快照（packages/shared/test/snapshot.test.ts）：record 形状 24 表 zod →
  JSON Schema 投影 + body/封套 + AgentBackend 缝/机器面 wire + 协议词表四族快照；
  #230 上收后 provider/agent/mcp 三族 create/patch 六件进 shared 单源快照面。
- 词表钉（vocabulary.test.ts）：phase 九值权威单源 / machine protocol（02 §5）/
  web REST 词表（02 §6.1）/ SSE channels / MCP 词表（02 §7.2 = r3 §6 matrix 1:1）/
  worker 记忆三件套 / 品牌槽常量（02 §5.8 + 素材替换计划 §2）。
- machine 面 13 端点词表不改形状：delta 第三形 = 既有路径载荷扩展 [设计]，
  MACHINE_WIRE_EXTENSIONS 机制同族登记。
- 全量 vitest：**52 文件 / 505 用例全绿**（server 25 / daemon 8 / web 7 /
  shared 4 / integration 8；12.99s，projects 并行）。

## 4. 行为清单逐条核（04 §4 场景表，V6）

| 组 | 场景 | 核验位 | 结果 |
|---|---|---|---|
| M2 | REST/SSE 面 CRUD、team stream 事件、搜索面板、定时 CRUD+触发闭环 | server test/wire.test.ts · sse.test.ts · schedules.test.ts · search.test.ts（505 用例内含绿）；web 搜索面板 live（客户端过滤真数据，search-focus / search-result-rows e2e 钉） | ✅ |
| M3 | 机器接入（claim~75s+wake）、worktree 契约、checkpoint/恢复、upload-urls、merge 202 delegated、free 1 台上限 | integration m3a-demo · m3b-demo · crash-recover（全绿）；m5-web-e2e 再证 merge 202→done 链 | ✅（crash-recover 已知 CI 偶发 flake——重跑确认口径，勿判回归） |
| M4 | Chief 六项（措辞→spec/分派权重/直派/watch-wake 三触发/驳回 v2+diff/双 Agent 分槽/memory 四件） | server chief.test · m4a-chief-loop · m4b-memory；integration m4a/m4b×3（全绿）；web chief drawer live 会话面（chief-panel / chief-settings e2e 钉） | ✅ |
| M5 | 端到端生命周期实跑（含驳回支线、定时轮停 review）、看板卡态流转、详情深层各态 | integration/m5-web-e2e.test.ts 本票重跑 3/3（9.57s）：①新建→规划→确认→执行→审核→合并→done（UI 零 reload + 会话流三事件族断言 + main 落地真值）②驳回→v2→版本对比 diff→确认→审核 ③once 定时→tick→直执行→停 review+「由定时发起」标记；reject-chain（fixture 面）e2e 绿 | ✅ |
| M6 | 真人一次项 | 见 §5 兑现记录 | ✅ 三项全兑现 |

web e2e 定点重跑（本票，E2E_PORT=8399）：reject-chain · notify-banner ·
provider-oauth · search-focus · search-result-rows · chief-panel ·
chief-settings 七 spec **35/35 绿**（9.6s）。

## 5. 真人一次项（M6 行）· 兑现记录

三项全兑现。通知两项 09-23 兑现（附录 A 指针），本票按全口径精神阶段重确：
代码路径在位核实通过——权限入口为应用内 banner（#114，notify-banner.tsx 驱动
`Notification.requestPermission()`；帐号页推送通知 switch 同路径），实弹路径
`sse.ts` = SSE notification → `document.hidden` 时页内 `new Notification()`
（plan_ready / build_review / chief_message 三型）。

**OAuth 真连号（2026-09-25，ego 浏览器驱动联调实跑）**：GitHub OAuth App
`pacman-local-m6` 注册 → Client ID/Secret 入 `.env` → 8795 端口起 server
（隔离数据根）→ 应用内「连接订阅」→ GitHub Copilot → 授权页 Authorize →
callback 302 回跳 → providers API 落行：`providerId = github-copilot`、
`baseUrl = https://api.githubcopilot.com`、`api = openai-completions`、
**无 apiKey 字段**（token 密封只写不读律成立）。V7 等价注记随本次连通摘除
（09-23 凭证链路等价路径记录随附录 A 归档）。

复跑/换族口径（就绪态操作表保留）：

| # | 项 | 操作（就绪态） | 预期观察点 |
|---|---|---|---|
| 1 | OS 通知权限弹窗人工允许（02 §9.1 / 04 §5） | `pnpm --filter @pacman/web build`（首回或有改动时）→ `pnpm dev:server`（web 同源托管，默认 8787）→ 开 `http://127.0.0.1:8787/app` → 看板顶部通知 banner 点「开启」（或帐号页推送通知 switch）→ 系统弹窗点「允许」 | banner 隐去；Console `Notification.permission === 'granted'` |
| 2 | 桌面通知实弹（04 §5 divergence 口径 A5） | 权限已允许后：开任一详情页 → **切页签到后台** → 触发通知事件（另一客户端建 todo 并保存并开始 → 规划完成即发 plan_ready；或等定时轮） | 页签后台期间桌面横幅弹出（标题/正文 = 事件文案）；切回前台页内未读徽标联动 |
| 3 | OAuth 真连号（#231 一族 = github-copilot） | 仓库根跑 `bash scripts/connect-github-copilot.sh`（五阶段 wizard：定 origin → 注册 GitHub OAuth App → 抄回 Client ID/Secret 入 `.env` → 带 env 起 server → 连号）；或手照脚本步骤 | 模型服务页列表出现 GitHub Copilot 行；回跳 URL 不带 `?oauth=error`（reason=denied = 授权页取消；reason=exchange = client 对/callback URL 不匹配） |

- 口径：族表现仅 github-copilot 一族，chatgpt/codex/xai 族扩展归后票（§6-12）。
- divergence 口径不变：不做 Web Push/VAPID（04 §5 已裁决，不判负）。

## 6. 登记面（divergence / [推断] / 后置）

1. **preview-token**：04 附录 B 在册不设计——无 UI/wire 触点，未实现（触发
   条件未至，非缺项）。
2. **浏览器授权页**（02 §5.2 路径一）：server 端 enroll/start/poll 骨架在位；
   web `/app/machines/authorize` 页未建——机器接入主路径 = CLI `--api-key
   --team`（路径二，E2E/文档口径），#181 已将「添加机器」落地为 CLI 两步表单
   弹窗（安装/执行命令块 + API key 引导），授权页本身归后票。
3. **streamdown（01/S7）**：transcript 渲染 = 自有分段渲染器（与像素契约 DOM
   统一——fixture/live 两模式同一呈现面，parity 行零风险）；富 markdown/数学面
   引入 streamdown 归后票评估（引入即 DOM 契约变更，需矩阵重验）。
4. **steer 位**（02 §5.6 词表）：daemon 缝在位（AgentSessionHandle.steer）；
   web building 态 composer 无 REST 写面（词表无端点）——发送仅 confirm 关口
   （revision）生效，building/review 态发送惰性。
5. **搜索服务端面**：GET /api/search 在位（02 §6.3 [设计] 自设）；面板契约为
   客户端过滤（live todos 真数据），服务端切换归后票。
6. **~~表单弹窗族后票~~（已消解）**：provider/secret/mcp/agent 创建弹窗全族
   live（POST 真写面 + e2e 钉 + parity open 态行 #229）；POST body schema 经
   #230 上收 shared 单源（create/patch 六件），web mutation 位收型
   Create*/Patch*Body——09-23 登记的两段随行义务均兑现。余项：API key 权限位
   表单弹窗仍归后票（新建密钥 = 默认权限位直发不变）。
7. **运行历史 tokens 位**：非当前行 meta 缺 tokens 帧——映射层
   （`mapRunHistory` 的 `tokensByBuild` 槽）与 per-build usage 端点均在位，
   调用方按需供数面未接，归后票；当前行/失败行/相对时间/错误串完整。
8. **帐号邮箱行**：复刻无邮箱账位面（02 §2.1 自动登录）——占位破折号 [设计]。
9. **conversation stream 载荷**：[推断] 定型（四事件），官方载荷补采触发 = 04
   附录 A 纪律（回写 02 §11）。
10. **i18n-coverage `api` 目录豁免**：数据层记录内容串与 fixtures 同族（zh-CN
    权威 canon，组件 t() 位兑现 en 兜底）——测试内注释登记。
11. **生产 bundle 内 fixture 表**：scenario 选择在生产 build 编译期折叠
    （#58 gate），live 数据路径不消费 fixture 内容字段；fixture 数据表本体仍在
    bundle 内（dev/parity 单源共用）——按动态 import 收编以减 bundle 体积归
    后票（纯体积优化，非数据面残留）。
12. **OAuth 族表单族**（#231 新增登记）：OAUTH_FAMILIES 现仅 github-copilot；
    chatgpt/codex/xai 一族入表 = 族表加行 + preset 钮随行，归后票。
13. **OAuth state TTL × handoff 间隔**（#232 连号实跑抓到的小 bug 证据）：
    state TTL = 10min（`OAUTH_STATE_TTL_MS`，[设计]「真人走一趟授权页足够」），
    首趟授权因 handoff 间隔超 TTL，callback 走 bad-state 分支 = 裸 400 JSON
    「oauth state expired」——不回跳是代码面有意形状（state 不在册时
    returnOrigin 不可信，routes.ts bad-state → HttpError 400）；重试（已授权态
    GitHub 直跳）秒过，功能不判负。裸 JSON 着陆体验归后票评估（静态错误页 /
    重授权引导方向）。

## 7. 升级票窗口事实（03 §3 节奏，四票已闭）

| 票 | 内容 | 落地 |
|---|---|---|
| #104 | TypeScript 5.9.3 → 7.0.2（工具链兼容面核实） | #117（i18n 门改 TS7 unstable/ast scanner，TS5 AST oracle 逐 token 等价核验；01 册 §4.5 pin 回写） |
| #105 | react-router 7.18.4 → 8.4.0（library mode 回归 + 路由形状保真） | #118（v8 Breaking 全缝外；01 册 §4.1 pin 回写） |
| #106 | Tailwind 3.4.19 → 4.x（dark 反相变体等价 + 全矩阵重验） | #119（@custom-variant 等价复刻 + @theme inline 迁移；parity 全矩阵重验 0 失败） |
| #107 | pi 0.85.1 → 0.86.0（D6 首个窗口：Breaking 段阅读 + AgentBackend 缝回归） | #115（Breaking 三条全缝外，纯 pin bump） |

现行 catalog 真值：typescript 7.0.2 / react-router 8.4.0 / tailwindcss 4.3.3 /
@earendil-works/pi-* 0.86.0——本票 parity 229 行 + vitest 505 用例全绿即四票
落定后的全口径重验。

## 8. 运行记录（2026-09-25 终态数字）

- parity：229/229 行 0 失败（67 门禁 + 143 smoke + 19 report-only；最低门禁行
  `detail-failed-light` 0.8459 @ 0.84）。
- vitest 全量：52 文件 / 505 用例全绿（12.99s，projects 并行；含 integration
  m3a/m3b/crash-recover/m4a/m4b×3/m5-web-e2e）。
- E2E 脊柱重跑：m5-web-e2e 3/3（9.57s）。
- web e2e 定点：七 spec 35/35 绿（9.6s，清单见 §4 尾段）。
- 三闸：typecheck 5 包全绿 / lint（biome ci）绿 / format 净。

## 附录 A · 2026-09-23 首轮兑现记录指针

首轮（M5 汇合 #83 + M6 真人一次项首兑现）全量记录见 git 历史：本册 @
`1c92cb6c`（2026-09-23，含权限允许/桌面实弹两真事件/凭证链路等价路径全链
build→merge→main 的逐步兑现记录）与 PR #108。本版按 #232 Q1 裁决 A 全口径
重跑覆盖，不再复述。
