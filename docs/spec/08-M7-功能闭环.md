# 08 · M7 功能闭环（2026-09-26 grilling 定案）

> 正典地位：本册 = M7 里程碑「功能闭环」的 spec 正本。方向框架承 06 册 D1
> （自主产品，复刻对拍义务已解除）；M0–M6 见 03 册（历史档案）。
> 裁决来源：2026-09-26 grilling 三轮定案（闭环标准 / 范围 / 分波 / 验证 /
> 语音处置 / 终点形态）。

## 0. 决议一览

| # | 项 | 定案 |
|---|---|---|
| C1 | 闭环标准 | **全量真闭环**：按钮背后功能前端→server→daemon 全通；唯一例外 = 语音输入不做 |
| C2 | 范围 | 当前已渲染的交互元素 + 反向差距（todos.dev 有而 pacman UI 没有的面）；继承 02 册排除项 |
| C3 | 分波 | 按依赖层：M7-W1 总账+补采 → M7-W2 纯前端 → M7-W3 server → M7-W4 daemon → M7-W5 重功能 |
| C4 | 盘点 | M7-W1 首票合并总账：r1/r2 功能清单 × 当前 UI × 05 册 §6 登记面 × 2026-09-26 死按钮扫描，四方对账 |
| C5 | 语音 | 两处语音按钮（composer / 新建任务对话框）移除；功能不做；无 parity 负担（06 册 D1） |
| C6 | 验证 | **每功能必 verify-pacman 证据**；feature map **按需**维护（功能面变化才同步，纯接线票不机械跑） |
| C7 | 终点 | M7 = 差距台账清零；每票 verify 证据在册 |

波次命名空间说明：本册 M7-W1..W5 为 M7 内部波次；06 册 D9 的 W1–W4+ 为产品波次（W1 门面、W2 首发已过）。06 册 D9「W3 steer 中断」收编为 M7-W4 的组成部分。

## 1. Problem Statement

真实使用中大量按钮点击无反应：composer 流式传输时停止钮无 handler、project 页空态新建钮无 handler、详情/资源/设置各面散布菜单桩与死钮。这些按钮在原版 todos.dev 中多为真功能（r8 §3.1 实测 AI 审核为完整 agent 回路），pacman 复刻了像素未接功能。06 册转自主产品后，功能闭环的取舍标准从「todos.dev 等价」转为「自有产品判断」，但「已渲染的交互必须生效」这一底线不变。

## 2. Solution

一次里程碑式收编：先合并四方来源出一本**差距台账**（唯一正账），再按依赖层分五波清零。处置范式沿用 #149/#163/#222 已立二分律——**有对象面 → 接真；无对象面 → wontfix 出账 + 移除渲染 + e2e 断言钉死**。每个功能面闭环必附 verify-pacman 真栈证据。

## 3. User Stories

核心路径：

1. As a 用户，I want 流式传输中点击停止钮能中断当前 build，so that 失控的 agent 步不必等超时
2. As a 用户，I want project 页空态的「新建任务」钮能开新建对话框，so that 空项目里能建第一个任务
3. As a 用户，I want 新建项目时「选择仓库」钮能列出可选 repo，so that 项目能绑定代码仓库
4. As a 用户，I want 看板/详情/资源各页的「更多」菜单钮都能开出真菜单，so that 次要操作可达
5. As a 用户，I want 详情页 transcript 的折叠钮能收起长消息，so that 长 build 记录可浏览

重功能（M7-W5，一功能一票）：

6. As a 用户，I want 待确认关口发起 AI 审核（选 Agent → 审核步 → blocking 自动修订），so that 方案质量有第二双眼睛（r8 §3.1 规格在册）
7. As a 用户，I want composer 与新建任务能添加附件，so that agent 能看到截图与文件（规格 M7-W1 补采）
8. As a 用户，I want composer 与新建任务能 @提及 todo/agent，so that 上下文引用显式化（规格 M7-W1 补采）
9. As a 用户，I want 新建任务对话框能添加标签，so that 任务分类在创建时完成
10. As a 用户，I want 分支对话框的「同步到机器」可选机器并执行同步，so that worktree 状态可控

反向差距（台账校准后转票）：

11. As a 用户，I want todos.dev 有而 pacman 缺失的功能面被盘点登记，so that 闭环边界是全书而非抽样

## 4. Implementation Decisions

### 4.1 波次结构

- **M7-W1 总账 + 补采**（单票，后续所有票的 blocking 前置）：
  1. 四方对账出差距台账（r1/r2 功能清单 × 当前 UI 交互面 × 05 册 §6 登记面 × 2026-09-26 扫描初值）；
  2. todos.dev 补采附件/提及两处交互规格（账号可用；r8 §3.1 先例的实测格式）；
  3. 后端能力盘点：server routes × daemon 能力 × 前端需求三方对账，W3/W4 的洞在此浮出，不留到施工期；
  4. 产出 = 本册附录 A 升格为正账 + 每行处置分支（接真 / wontfix 移除）+ 各票输入。
- **M7-W2 纯前端**：档 3 菜单桩 + 档 4 外链处置 + 档 1 纯前端部分（空态新建接 onNewTask、选择仓库接 repo 列表）。无 server/daemon 依赖，票间无 blocking 边，可并行。
- **M7-W3 server**：停止（steer 端点）、附件上传/存储、提及解析、标签写路径等端点补全。
- **M7-W4 daemon**：steer 中断（收编 06 册 D9 W3）、附件同步、AI 审核执行能力。
- **M7-W5 重功能**：AI 审核 / 附件 / 提及，一功能一票，开工前重新切片评估。

### 4.2 处置二分律（沿用 #149/#163/#222 先例）

每行台账只有两种终态：
- **接真**：对象面存在（local-first 可承载）→ handler 接真链路；
- **wontfix 出账**：无 local-first 对象面 → 移除渲染 + 实现位注记 + `dead-buttons.spec.ts` 断言钉死。

### 4.3 语音

composer 与新建任务对话框的语音按钮移除（C5）。例外的例外：若 M7-W1 补采发现提及/附件交互与语音按钮强耦合（同一 toolbar 布局假设），由 W2 票内顺带调整，不单独立项。

### 4.4 缝

不新建缝。最高缝 = verify-pacman（隔离栈真用户路径）；其下沿用 integration（stub-llm 链路，server×daemon 协议）、apps/web e2e（fixture 行为断言）、vitest。parity harness 身份承 06 册 D1 = 回归工具，M7 不消费其「平价门禁」语义。

## 5. Testing Decisions

- 好测试 = 只测外部行为（按钮点击后的真实效果：API 调用、状态迁移、UI 反馈），不测实现细节。
- **每功能 verify-pacman 证据**（C6）：截图 + API JSON + SQLite 行落 `.claude/verify-evidence/`。
- wontfix 移除类处置沿用 `apps/web/e2e/dead-buttons.spec.ts` 钉断言（先例 #149/#222）。
- server/daemon 协议层走 integration stub-llm 链路（m4b 先例）。
- feature map 按需维护（C6）：功能面变化的票跑 maintain-verification-skill，纯接线票不跑。
- 测试规则承 AGENTS.md：禁补单测、E2E 为主、先列失败方式再写实现。

## 6. Out of Scope

- 语音输入（功能不做，按钮移除——C5）。
- 02 册排除项：付费墙/多租户/注册邀请/invite 流/云端多租隔离。
- streamdown（07 册：暂缓 + 触发线）；preview-token（05 册 §6：触发条件未至）。
- todos.dev 基线重拍与像素对拍（06 册 D1 已解除对拍义务）。
- 本轮台账之外的新功能创意（涌现制归 06 册 D9 W4+）。

## 7. Premortem 护栏（假设 M7 失败的三死因）

1. **总账漏项**：r 文档实测覆盖不全，施工后期又冒新洞 → 护栏：M7-W1 验收 = 四方对账 + 补采顺带全站扫描 + 后端能力盘点三合一时才可封账。
2. **M7-W5 重功能失控**：AI 审核为完整 agent 步（模态+审核步+自动修订回路），三功能塞一波必烂尾 → 护栏：一功能一票，M7-W5 开工前重新切片。
3. **证据表演化**：票多证杂，verify 沦为补截图 → 护栏：证据 = feature map 条目 + 最小关键截图，不全量；纯接线票允许 e2e 断言替代 verify 栈（处置行为可由 fixture 断言覆盖时）。

## 附录 A · 差距台账（正账，M7-W1 #303 定稿 2026-09-26）

> 四方对账来源：r1/r2 功能清单 × 当前 UI 交互面（#303 逐文件核实，含
> button/onClick 全库清点）× 05 册 §6 登记面 × 2026-09-26 静态扫描初值；
> 校准补采 = r9（`docs/research/r9-attachments-mentions.md`，todos.dev 登录态
> 实测）。处置二分律见 §4.2；每行归属票号即 M7 清零验收单位（C7）。
> 「已实」行 = 档 5 未验证区的核实结论，无需施工。

### 档 1 · 核心功能洞

| 行 | 位置 | 处置 | 归属 | 规格 |
|---|---|---|---|---|
| composer 停止钮 | `detail/composer.tsx` composer-stop（streaming 渲染、无 onClick） | 接真 | #308 | r9 §3.3：确认弹层「停止当前这一轮？」+ 默认勾选「丢弃本轮修改」+「正在停止…」过渡 + 运行行「已取消」+ todo 落上一完成 turn 的 gate；r1 changelog 09-15/09-16 |
| project 页空态新建钮 | `pages/project-page.tsx` prj-tasks-empty-new（无 onClick） | 接真 | #305 | r2 24b（空态 → 新建任务对话框） |
| 新建项目选择仓库钮 | `pages/project-new-page.tsx` prj-new-repo（无 onClick） | 接真 | #305 | r2 07b/07c（仓库选择弹窗 → 创建提交携带） |

### 档 2 · 重功能未接

| 行 | 位置 | 处置 | 归属 | 规格 |
|---|---|---|---|---|
| AI 审核 ×1 | `detail/composer.tsx` aiReview 钮（无 onClick；显隐律已实装 = confirm/review 且非 awaitingReply） | 接真 | #312 | r8 §2.5/§3.1（模态 560 → 选 Agent → 审核步 → findings → blocking 自动修订） |
| 附件 ×2（composer + 新建任务） | composer Paperclip 钮 + `overlay/new-task-dialog.tsx` 工具条钮（均无 onClick） | 接真 | #310 | r9 §3.1/§4：`POST /api/uploads/grant` → 独立 upload host（FormData）→ content 序列化 `![名](attachment:attachments/{teamId}/{id}.{ext})`；新建任务 = 落描述随 spec |
| 提及 ×2（composer + 新建任务） | 同上两处 Grid2x2 钮（均无 onClick） | 接真 | #311 | r9 §3.2：五分组弹层（任务/技能/Agents/项目/机器，多选累积 + 插入 (N) 底条）+ composer 键 `@` 内联 agents 补全；序列化 `@名`→`[名](agent:{id})`、任务 `#seq` |
| 新建任务添加标签 | `overlay/new-task-dialog.tsx` new-task-tag-add（无 onClick） | 接真 | #309 | r9 §3.4：标签面板（pill toggle + 新建内联表单）→ `POST /api/projects/{id}/tags {name,color}` → createTodo body 携 `tagIds`；fresh 详情 meta 区 chip 渲染。**校准：看板卡不渲染标签（r9 实测，#309 AC 的卡面假设修正）** |
| 分支对话框同步+机器选择 | `detail/branch-dialog.tsx` dlg-machine（无 onClick）+ dlg-sync（恒 disabled） | 接真 | #319 | r1 changelog 09-13：机器选择器 + sync 目录 + force 开关 + pending→running→synced/failed 结果卡；任意团队成员可用 |

### 档 3 · 菜单/导航桩

| 行 | 位置 | 处置 | 归属 | 规格 |
|---|---|---|---|---|
| chief 抽屉「更多」 | `chief/chief-drawer.tsx` hasThread ⋮ 钮（无 onClick） | 接真 | #306 | r9 §3.5：更换图标 / 重命名主题 / 删除主题（r1 changelog 09-08 的 UI 入口） |
| schedules 卡片「更多」 | `pages/schedules-page.tsx` sched-card-more（无 onClick） | 接真 | #306 | r9 §3.5：编辑 / 移除（移除无确认弹层；server DELETE 已在） |
| skills 排序 | `resources/skills-page.tsx` res-sort（无 onClick） | 接真 | #306 | 原站点击行为未捕获（r7 08）；语义 [设计] 同 prj-tasks-menu 家族（客户端排序） |
| account-swap | `routes/account-page.tsx` account-swap（无 onClick；#148 已裁 wontfix 但按钮仍渲染） | wontfix 移除（补完 #148 裁决） | #306 | #136/#148 在案：静态头像资产无上传面 |
| plan-dropdown 行 | `overlays/plan-dropdown.tsx` 单行无 onClick = **原站同形**（r5b 05f 型选标记非确认入口，无差距）；`detail/docpane.tsx` VersionMenu 版本行 onClick=仅关菜单（不切版本，原站行为未采） | 型选无差距；版本行语义 [设计] 定夺（切显示版本 or 维持关菜单） | #306 | r5b 05f / r8 63（版本行点击未采） |
| transcript 折叠 | `detail/transcript.tsx` chat-collapse 收起钮（无 onClick） | 接真 | #306 | r8 63–77 工具组展开/收起族（客户端状态） |
| 【校准新增】更多菜单完成/关闭行 | `overlay/more-menu.tsx` 完成/关闭 onClick=onClose（dismiss 桩；「等后端线」注已过期） | 接真 | #318 | r1 changelog 09-16：Complete = confirm-and-merge（相位适配）；Close = 关闭语义（延迟 Undo 窗口归票内裁量） |
| 【校准新增】开始任务 dialog 统一面 | `detail/overlays.tsx` RerunDialog rerun-agent-row 死钮（被 backdrop div 的 onClick 计数掩盖）+ 分用开关静态 span；待开始相位直接 startBuild 无 dialog | 接真 | #318 | r9 §3.6：Agent 行选择器（含未指派）+ 机器行（自动/指定+在线点）+ 分用开关 ON=规划/执行双 Agent 行（正对 server assignment 双槽）+ 先做规划/立即执行 |
| 【校准新增】复用方案「查看方案」 | `detail/overlays.tsx` ReusePanel onView=closeOverlay（桩） | 接真（显示形态 [设计] 裁定） | #318 | 原站行为未采（r8 §5 登记；r9 §5 复登）；plans 读面已在 |
| 【校准新增】项目任务行无点击 | `pages/project-page.tsx` prj-task-row / prj-task-card（div） | 接真（导航 /app/todo/:id） | #318 | r2 §2（原站点行开任务详情） |
| 【校准新增】新建任务未保存闸 | `overlay/new-task-dialog.tsx` 关闭直关 | 接真 | #318 | r9 §3.4：「放弃新建任务？未保存的内容将丢失。」确认弹层（继续编辑/放弃并关闭） |

### 档 4 · 文档/外链型（wontfix 移除）

| 行 | 位置 | 处置 | 归属 | 规格 |
|---|---|---|---|---|
| keys-docs | `routes/api-keys-page.tsx` 查看文档钮（无 onClick） | wontfix 移除 | #307 | #149 先例（local-first 无文档站） |
| res-doclink | `resources/parts.tsx` EmptyState 查看文档（共享位，覆盖全部资源空态） | wontfix 移除 | #307 | #149 先例 |
| create-agent swap | `routes/create-agent-dialog.tsx` dlg-agent-swap（无 onClick，注记在位） | wontfix 移除 | #307 | #136/#148 族：无上传面 |
| prj-set-change | `pages/project-settings-page.tsx` prj-set-change（无 onClick，注记在位） | wontfix 移除 | #307 | #177/#148 族：无上传面 |

### 档 5 · 未验证区（#303 全部核实转实）

| 区 | 核实结论 | 状态 |
|---|---|---|
| dhead | 返回导航（携 search）/chip popover（#67/#209）/文档\|聊天 tabs/更多→MoreMenu/branch/token/history overlay/主按钮（开始/确认/完成/重开/重跑 按相位分流 live mutation）——全接真 | 已实 |
| docpane | 型选 listbox（#149）/版本菜单+对比（r8 65–72 live）/全部展开收起/显示完整文件（#225/#244）——全接真；版本行 close-only 归档 3 | 已实 |
| overlays 族 | board-guide（#149 真内容弹层）/chip-popover（编辑分配 #209）/search-panel（live 服务端搜索 #286）——全接真 | 已实 |
| sidebar 族 | 导航 Link 族/侧栏折叠持久（#55）/项目资源分组折叠（#147）/用户菜单/⌘K——全接真 | 已实 |
| schedules live | GET+POST live（05 §1.3，SSE todo 联动）；卡片级编辑/移除归档 3 | 已实（卡级归 #306） |
| 其余复核 | board/todo-card（开始→startBuilds、分支图标→overlay）/user-menu（主题）/team-page（布局+创建 Agent）/chief-settings（patchChief 三面）/chief-wake/form 弹窗族（provider/secret/mcp/agent/api-key/machine #181）/token-gate（表单提交）/skills-import（#233/#235）——onClick 计数复核 + 05 §1.3 交叉，全接真 | 已实 |

### 反向差距与登记面（r1/r2 × r9 校准，非死钮）

| 行 | 现状 vs 原站 | 处置 | 归属 | 规格 |
|---|---|---|---|---|
| chief composer 附件/提及 | pacman 已按 #136 wontfix 移除；原站 chief composer 实有（r9 §3.5：语音/添加附件/提及三钮） | 票内裁量（回插与否——spec §3 story 7/8 只点名 composer 与新建任务） | #310/#311 | r9 §3.5 |
| 失败面发消息 | pacman：failed 相位 composer 可写但 onSend 无 failed 分支 = **静默丢稿**；原站实测 = 发送触发新一轮（消息随新轮入会话） | 接真 | #320 | r9 §3.3 |
| steer 端点分叉 | 原站专用 `POST /api/conversations/{id}/steer`；pacman 走 messages 端点 build 分支（#280）——载荷同义、路径分叉 | [设计] 注记（对齐归票内裁量） | #308 | r9 §4 |
| 名称编辑 glyph | account/project-settings SquarePen = 静态 span 渲染；原站行为未捕获（r2 §9-20 未测）、server 无 PATCH user/me 与 PATCH projects | 维持静态 + 注记（分支 chip #149 静态化同律；补采证实原站可编辑再转票） | —（无需施工） | r2 §11-20 |
| 详情路由形态 | 原站现 /app/todo/:id 独立路由（r2 期 panel=todo: 查询参形态已漂移）；pacman 已同形 | 无差距 | —（无需施工） | r9 |

## 附录 B · 后端能力盘点（server × daemon × 前端需求，#303 定稿）

> server 现状 = 75 条路由全枚举（`apps/server/src/routes.ts`）+ services 面精读；
> daemon 现状 = machine wire 13 端点 + machine-loop/AgentSessionHandle 精读；
> 需求 = 附录 A 各行。W3/W4 的洞在此浮出。

| 需求 | server 现状 | daemon 现状 | 缺口 | 票 |
|---|---|---|---|---|
| 停止/steer | steer 写面**全链在**：`POST /api/conversations/{id}/messages` build 分支（sendBuildSteer：claimed 步门 409 + steer_pending 单槽 + machine steer 信号，#278/#280）+ `GET /api/machine/steer` 拉取确认 | machine-loop steer 事件→拉取→`live.steer()` 投递在（#279） | **stop 路径全缺**：`AgentSessionHandle.stop()` 接口在但无 server 端点 / wire 事件 / daemon 分流；中断落账（运行行「已取消」+ gate 回落 + rewind——M3 checkpoint/restore 基建在） | #308 |
| 标签 | `GET /api/projects/{id}/tags` 在；`PATCH /api/todos/{id}` tagIds 槽在（todo_tag join 全替换） | 不涉及 | `POST tags {name,color}`（tag record 缺 color/createdAt/v 三位）；createTodo body 缺 tagIds；标签删除/管理面（原站 = 项目设置「标签」tab）；web 面 | #309 |
| 附件 | 仅词表桩：chief-tools/mcp-face `attachment` 工具回 `attachment store not wired`；无表无端点无上传面 | 无 | **全栈**：uploads grant 端点 + 存储 + content 序列化（`attachment:` markdown）+ spec/消息携带 + agent 侧读取（工具往返）；wire 规格 = r9 §4 | #310 |
| 提及 | 零 | 零 | **全栈**：实体解析（`agent:` scheme markdown）+ transcript chip 渲染（chief Segments `#N`/agent chip 基建可复用）+ 选择器 UI；规格 = r9 §3.2 | #311 |
| AI 审核 | steps action 词表仅 revision/confirm；step kinds = plan/build/merge | 步执行循环在（claim/heartbeat/done/upload-urls） | 审核 action + 审核步 kind + findings 消息 wire + blocking 自动修订回路（可复用 revision 机器：feedback 注入重规划） | #312 |
| 分支同步 | machine wire 13 端点无 sync 族；无 server 命令位 | 无 sync 命令（git/worktree 基建在） | server 同步命令位 + pending/running/synced/failed 状态落账 + wire 事件 + daemon git 同步执行 + web 机器选择 | #319 |
| 更多菜单完成/关闭 | merge 端点在（review→done，202 delegated）；PATCH phase 漏斗 `todo/failed→closed` 在 | merge 步在 | `review/confirm/done→closed` 漏斗边（原站 \*→closed）；前端菜单行接线 | #318 |
| chief ⋮ 三行 | threads GET/POST 在 + `PATCH /api/teams/{id}/chief`（charter/compaction 等）在 | 不涉及 | thread 级 PATCH（title/emoji，r1 changelog 09-08）+ DELETE thread | #306 |
| 定时卡片 ⋮ | `GET/POST/DELETE /api/schedules` 全在 | 不涉及 | `PATCH /api/schedules`（编辑）；前端菜单（r9 §3.5：编辑/移除） | #306 |
| 失败面发送 | startBuilds 在；failed→queued 漏斗边在 | 步循环在 | 失败面发送动作位（原站 wire 形未采，r9 §5——实走 steps 端点 body 未录）；前端 failed 分支 | #320 |
| 其余 | 机器/搜索/通知/OAuth（github-copilot 族）/skills（含 scan）/projects（tree/file/commits/DELETE）等已在（05 册 §1.2/§1.3） | 13 端点全在（enroll/claim/heartbeat/tool/token/upload/done/steer） | —（M7 范围外项见 §6 与 05 册 §6） | — |
